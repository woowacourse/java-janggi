# 규칙
블랙잭 미션에서 가져온 규칙을 포함한 장기 미션 규칙입니다.

## 테스트

#### [테스트 단위]
(테스트 단위 기준) 기능 단위로 테스트한다. (기능: 외부에서 기대하는 동작)

(If-Then) 기능을 수정하지 않았는데 테스트마다 결과가 달라진다면

→ 랜덤 로직을 외부에서 주입하고, 해당 테스트에서는 고정 값으로 테스트한다.

#### [테스트 제외]
(금지) 입출력기능(Scanner, sout)자체는 테스트하지 않는다.

(금지) private 키워드를 가진 메서드는 테스트하지 않는다.

단, 테스트가 필요한 private 메서드가 있다면, 해당 책임을 수행하는 새로운 객체를 생성하여 public 메서드로 만든다.

---
## 설계

### [책임 분리]
(기준) 책임 분리 기준: 객체가 본인의 상태만 사용하는가

### [변경 범위 제한]
변경 범위 제한 규칙

(If-Then) 만약 각 레이어(ex 도메인-컨트롤러 or DB-도메인)가 서로 의존하고 있다면

→ 중간 레이어(ex Dto)를 활용하여 의존성을 끊는다.

(기준) 변경 범위 제한 규칙: “레이어들이 서로를 모르게 구현한다.”

(If-Then) 만약 새로운 요구사항을 반영하기 위해 기존에 잘 돌아가던 코드를 직접 수정하고 있다면

→ 개방-폐쇄 원칙(OCP)을 위반한 것이므로, 기존 코드 수정 대신 새로운 클래스나 메서드를 추가하는 방식으로 확장이 가능하도록 재설계한다.

### [상태 소유]

(If-Then) 특정 객체의 상태를 외부에서 조건문으로 조합하여 판단하고 있다면

→ 변경의 책임이 있는 객체가 그 상태를 소유하도록 한다.

### [불변 캡슐화]
(If-Then) 특정 객체가 setter 혹은 그에 준하는 메서드를 가지고 있다면

→ 해당 메서드를 없애고 새로운 객체를 만들어 반환하도록 수정한다.

(기준) Entity를 제외한 모든 객체를 불변으로 만든다.(실험해보자)

---
## 다형성

### [캡슐화 기준 (무엇을 숨기는가)]
(If-Then) 특정 객체의 상태를 활용한 로직이 외부 객체에 있다면

→ 해당 로직을 객체 내부로 옮기고, 객체에게 메시지를 던지는 방법으로 수정한다.

*특정 객체의 상태를 활용한 로직: if문과 getter로 조합해서 특정 객체의 상태나 행동을 정의하는 경우

### [null 사용 기준]
(If-Then) null을 처리하는 로직이 생긴다면

→ null의 상태와 행동을 정의하는 빈 객체로 대체한다.

### [역할/인터페이스 설계 기준]
(If-Then) 3개 이상의 객체에게 동일한 메서드(메시지)를 던지고 있다면

→ 메시지를 인터페이스 혹은 상위 abstract 클래스로 추상화한다.

### [조건문 대체]
(If-Then) 외부에서 if문으로 객체 내부의 상태를 조합해서 행동을 결정하는 로직들이 있다면

→ 캡슐화가 덜 되어 있다는 신호이니 상태를 객체로 만들고 그 행동을 객체 내부에서 정의한다.
