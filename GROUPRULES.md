## 그룹 규칙

---

### [상태 위치 규칙]: 어떤 상태를 어떤 객체가 가지는가?

- 객체의 행위를 먼저 정의하고, 그 행위가 다루는 상태는 객체 내부에서 관리한다.

### [불변 객체 기준]: 언제 불변으로 만드는가

- 최초 생성 이후, 객체 내부의 상태 변경이 되면 안 되거나 외부에서 변경하지 않는 경우, 불변 객체 사용을 고려할 수 있다.

### [캡슐화 기준]: 무엇을 숨기는가

- 객체 내부의 상태와 본인만 알아야 하는 행위를 숨기고, 외부에서 직접 상태를 변경할 수 없도록 한다.
- 외부에는 협력에 필요한 메소드와 상태에 대한 간략 요약 혹은 연산 결과만 노출한다.
- (수정)상태가 변경되지 않는 경우에는, 외부에 프로퍼티를 노출시켜도 괜찮다.

### [null 사용 기준]

- null 사용은 최대한 지양하며, 대신 명시적으로 알아볼 수 있는 별도의 타입(객체)을 사용한다.
- 불가피하게 null을 사용하는 경우에는 Optional 등으로 안전하게 감싸거나 검증을 거치고 사용한다.

### [조건문 대체 기준] - 언제 다형성으로 바꾸는가

- 하나의 객체 안에서 여러 상태를 관리하는 로직이 많아져 분리가 필요할 때, 공통 행위를 객체별로 다르게 수행하도록 다형성으로 바꿀 수 있다.

### [역할/인터페이스 설계 기준]

- 중복되는 행위가 발생할 때 내부 속성이 아닌 행위(메소드)를 기준으로 인터페이스를 먼저 정의하고, 공통된 것들을 역할과 행위로 추상화한다.
- 공통 행위를 객체별로 다르게 구현하거나, 상황에 따라 구현체를 다르게 조립하고 싶을 때 인터페이스를 활용한다.

### [새 타입 추가 시 변경 범위 제한 규칙]

- 새로운 타입이 추가될 때는 추상체(인터페이스 등)가 있는지 먼저 확인하고, 가능한 해당 객체 내부와 생성하는 곳만 변경한다.
- 추상체가 있다면 새 타입 외에 다른 변경이 발생하지 않도록 하고, 다른 객체에 영향을 준다면 리팩토링을 고려한다.

### [테스트 규칙]

- 테스트는 메서드가 아닌 기능(하나의 역할) 단위로 작성한다.
- private 메서드를 직접 테스트하고 싶다면 책임 분리를 의심한다.
- 단순히 객체의 타입만 반환하는 getter 메서드 자체를 독립적으로 테스트하지 않는다.
- 입출력, 컨트롤러 등 어플리케이션 계층은 테스트하지 않는다.
- 메서드에 분기가 있다면 경계 값과 분기별 테스트를 나눠 작성한다.

### [책임 분리 규칙]

- 다른 이유로 변경되는 코드는 서로 다른 책임으로 보고 분리한다.
- 객체의 상태는 객체 스스로 변경하도록 하고 외부에서 직접 변경하지 않는다.
- 데이터를 꺼내 판단하지 않고, 객체에게 상태 변경/요약의 행동을 위임한다.
- 도메인 객체는 UI, 입출력, 프레임워크에 의존하지 않도록 한다.
- import를 통해 불필요한 의존이 있는지 확인하고 책임이 과하지 않은지 점검한다.
- 일급 컬렉션이 처리해야 할 로직이 외부에 있지 않은지 확인한다.

### [리팩토링 규칙]

1. 역할/책임 분리를 가장 먼저 수행한다.
2. 중복 로직을 제거한다.
3. 검증 로직이 필요한 필드 값은 Value Object로 만들어 클래스로 감싼다.

### [도메인/저장소 분리 기준]

- 만약 도메인 객체가 JDBC/SQL을 직접 호출하면 Repository 인터페이스를 도입하고 구현을 분리한다.

### [저장 경계 규칙]

- 모든 DB 저장 로직은 도메인과 DB 사이에 있는 Infra 영역에서 관리한다.
- 만약 저장/조회 로직이 게임 조율 로직과 섞이면, 서비스 계층에서 저장소를 호출한다

### [저장 시점 규칙]

- 객체가 생성됐을 때 저장한다.
- 각 턴 종료 시점에 저장한다.