# 타스, 와이제리 의사결정 기록 모음

> 고민되는 사안에 대한 의사결정 기록을 모아두는 곳

```
2026-03-25: <주제>
- 고민:
- 결정:
- 이유:
- Etc:
```

```
2026-03-25: 정적 팩토리 메서드 vs 생성자 인스턴스 생성 위치
- 결정: 생성자에서 인스턴스 생성 결정
- 이유: 비즈니스 로직을 담은 인스턴스 생성이기 때문에 생성자에서 보장하겠다.
```

```
2026-03-25: 졸의 팀을 상태로 가질지??
- 결정: 졸을 초/한 둘의 각각 다른 객체로 생성했다. (이동 방향 고려)
- 이유: Team 의존하지 않게 하기 위해 결정.
```

```
2026-03-26: isMovaable() 메서드의 책임
- 고민: 
    - 와이제리: isMoveable()은 기물 규칙으로서 갈 수 있는 방향에 대한 것만을 묻는 것이다. 예를 들어 포의 상하좌우만 판단하고 바로 앞 한 칸을 못 가는 여부는 판단하지 않는다 라고 생각함.
    - 타스: isMoveable()은 기물 내에서 기물이 이동할 수 있는지만 묻는다. 외부의 상황이 모두 충족된 상태를 가정했을 때 기물이 이동 가능한 능력을 의미
- 결정: "기물이 갈 수 있는 목적지"라고 생각한다면 포의 현재 위치 상하좌우 앞 한 칸 또한 배제해줘야 한다.
- 이유: 
```

```
2026-03-26: isMovaable() 메서드의 책임
- 고민: 
    - 와이제리: isMoveable()은 기물 규칙으로서 갈 수 있는 방향에 대한 것만을 묻는 것이다. 예를 들어 포의 상하좌우만 판단하고 바로 앞 한 칸을 못 가는 여부는 판단하지 않는다 라고 생각함.
    - 타스: isMoveable()은 기물 내에서 기물이 이동할 수 있는지만 묻는다. 외부의 상황이 모두 충족된 상태를 가정했을 때 기물이 이동 가능한 능력을 의미
- 결정: "기물이 갈 수 있는 목적지"라고 생각한다면 포의 현재 위치 상하좌우 앞 한 칸 또한 배제해줘야 한다.
- 이유: 
```