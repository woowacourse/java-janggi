package piece;

import move.MoveStrategy;
import move.Position;

public class Chariot extends Piece{
    public Chariot(MoveStrategy moveStrategy, Position position) {
        super(moveStrategy, position);
    }
}
