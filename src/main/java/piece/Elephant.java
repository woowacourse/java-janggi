package piece;

import move.MoveStrategy;
import move.Position;

public class Elephant extends Piece {

    public Elephant(MoveStrategy moveStrategy, Position position) {
        super(moveStrategy, position);
    }
}
